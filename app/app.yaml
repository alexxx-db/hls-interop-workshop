command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8501"

env:
  - name: CATALOG
    value: "hls_workshop"

resources:
  - name: sql-warehouse
    sql_warehouse:
      permission: CAN_USE
